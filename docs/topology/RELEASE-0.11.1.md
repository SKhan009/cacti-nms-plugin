# 0.11.1 — Remove the Evidence page

Removed LLDP / CDP Evidence from the shared sidebar template and retired its page rendering. Existing topo_links.php bookmarks redirect through authentication to Topology View. Discovery collection and stored observations remain available to the topology backend. Native device records, configured cables and layout are preserved.
